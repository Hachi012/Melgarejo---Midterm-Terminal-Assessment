// Firebase configuration for STRM app
// To enable Firebase on Web:
// 1. Create a Firebase project at https://console.firebase.google.com
// 2. Get your project credentials
// 3. Replace the config object below with your Firebase config
// 4. Uncomment the Firebase initialization code

export const firebaseConfig = {
  // Replace with your Firebase project config
  // apiKey: "YOUR_API_KEY",
  // authDomain: "your-project.firebaseapp.com",
  // projectId: "your-project",
  // storageBucket: "your-project.appspot.com",
  // messagingSenderId: "YOUR_SENDER_ID",
  // appId: "YOUR_APP_ID"
};

// Initialize Firebase when web Firebase SDK is loaded
// This will be called automatically if you configure the config above
